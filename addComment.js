import React, {Component} from 'react',
import import { Card, CardImg, CardImgOverlay, CardText, CardBody, CardTitle } from 'reactstrap';


class addComment extends Component {
    constructor (props){
        super(props);
        const commentNumber = this.campsite.comment
    }
}